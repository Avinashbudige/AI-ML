# %% [markdown]
# Notebook Title: <short descriptive title>
# Objective:
# - What you'll show/demonstrate (1-2 sentences)
#
# %% [markdown]
# Setup
# - packages and seed
# %% 
import numpy as np
import pandas as pd
import random
import matplotlib.pyplot as plt
import seaborn as sns
SEED = 42
np.random.seed(SEED)
random.seed(SEED)

# %% [markdown]
# Data download / load
# - Instructions to fetch dataset (small and reproducible); show sample head
# %%
# df = pd.read_csv("data/example.csv")
# df.head()

# %% [markdown]
# EDA & Basic Stats
# - visualizations and short interpretations

# %% [markdown]
# Preprocessing / Feature engineering
# - show pipeline steps (transformers or custom functions)

# %% [markdown]
# Baseline model(s)
# - code to train a simple baseline (sklearn) and evaluate

# %% [markdown]
# Improvements / advanced model
# - what you tried and why; show tuning summary

# %% [markdown]
# Evaluation
# - metrics, confusion matrix/ROC and short interpretation

# %% [markdown]
# Conclusion & Next steps
# - bullet list with clear next actions and limitations
#
# %% [markdown]
# Reproducibility notes
# - hardware note, approximate runtime, commands to reproduce
